;; ----------------------------------------------------------------------------
;; General Settings
;; ----------------------------------------------------------------------------
(provide 'user-info)

;; possibly set all program files as constants (there shouldn't be too many)
;; (defconst PROG-RTERM "C:/program files/R/R-2.11.1/bin/Rterm.exe")

(setq user-full-name	"John P. Hilbert"
      user-mail-address "jphilbert@gmail.com")


